<div class="category-row">
  <h4><%= category.name %></h4>
  <div class="admin-separator"></div>
  <div class="ui four stackable cards">
    <%= render partial: 'products/product', collection: category.products.by_name, cached: true %>
  </div>
</div>